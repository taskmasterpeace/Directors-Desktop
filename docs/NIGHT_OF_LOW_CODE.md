# Night of Low Code

One small, surgical improvement per entry — a single commit each, minimal
lines, maximum leverage. Running on a 15-minute loop (2026-08-06). Changes go
live in the installed app at the next installer swap.

| # | Improvement | Lines | Commit |
|---|-------------|-------|--------|
| 1 | Playground prompt helper now says **"Type @ to drop in your Characters & References"** — Robert hunted for his characters tonight and the machinery was there all along, invisible. One string. | 1 | `2e1d9a2` |
| 2 | Gallery's last two off-brand blues → amber: the loading ring and the model badge on every card. The app speaks amber everywhere else; these were LTX leftovers. | 2 | `3947b02` |
| 3 | Deleted `QueueTimers.tsx` — 92 lines of dead code. Nothing imported it since the Playground queue strip replaced it, and its hardcoded model-name map was a stale-data trap waiting for the next reader. | −92 | `f9d0c3e` |
| 4 | Gen Space now shows the **hot/cold warmth pill** when a local video model is selected — same honest residency story the Playground got. A cold engine costs ~5-15 min of load before the render; Gen Space was the one surface that still hid that. | ~13 | `9909618` |
| 5 | **Esc closes the Enhance Director modal.** Every other overlay honors Esc (queue edit, lightbox); this one trapped you into reaching for the mouse mid-flow. | 9 | `f26dd12` |
| 6 | **Copy-prompt button in the Gallery lightbox**, next to Remix. Remix reuses a prompt in-app; Copy gets it OUT — into Palette, a chat, notes. Check-mark flash confirms. | ~15 | `4157def` |
| 7 | **"clear finished" on the queue strip's done-trail.** Finished rows lingered until app restart; now one click hides them (the renders stay in the Gallery). Session-scoped dismiss set. | ~14 | `c4895d7` |
| 8 | **Failed renders now show IN the queue strip** — red row with the actual error, a Retry (fresh seed, reuses the duplicate path), and dismiss. Before this a dead job just vanished; the strip only rendered running/queued/complete, which is exactly the "I don't know if it's still going" hole. | ~40 | `49ac4ed` |
| 9 | **"Open folder" in the Gallery header** — reveals the newest render in Explorer via the existing `showItemInFolder` IPC. The literal answer to "where do my generations actually live," one click from the surface that shows them. | ~18 | `8dd39e6` |
| 10 | **Drag-highlight blues → amber** on the gen surfaces: Playground FrameSlot + Gen Space's two image drop zones flashed LTX blue mid-drag. (Checked first whether FrameSlot even had drag-drop — it does, plus paste, done right.) Observed but deferred: a full app de-blue is ~75KB of hits (ExportModal, VideoEditor, ConfirmDialog…) — that's a themed sweep for a real work session, not a low-code night. | 3 | `ca372dd` |
| 11 | **Edit banner now states the kept seed** ("Keeps seed N — same seed + same prompt = same take"). First AUDITED whether editing rerolled the seed — it doesn't (`...editingJob.params` spread preserves it); the fix is telling the user, since identical-looking takes were the original seed mystery. | 3 | `5a7b6b2` |
| 12 | **Gen Space placeholders now hint @** ("type @ for your Characters") — the full @-autocomplete was already wired on this surface too, and just as invisible as the Playground's was before row 1. Checked along the way: queue-strip buttons all titled, lightbox already autoplays, Then-Animate already states its model, logs reachable via LogViewer. | 2 | `80abdd4` |
| 13 | **Bug fix on row 8**: the failed-row filter matched `status === 'failed'`, but the backend's terminal literal is `'error'` (`job_queue.py` Literal) — so the row NEVER fired. Caught while cross-checking use-generation's error handling. Lesson recorded: status strings must come from one shared source, not memory. | 1 | `8d18111` |
| 14 | **`QueueJob.status` is now the union literal** (`queued\|running\|complete\|error\|cancelled`, mirroring `job_queue.py`) instead of `string` — the row-13 bug class is a compile error from here on. tsc surfaced zero other violations; vitest 245 green. | 3 | `1041d96` |
| 15 | **Windows taskbar progress during renders** — running fills the icon with the real fraction, queued pulses indeterminate, terminal ticks clear it. A 5-15 min render is now glanceable from the taskbar while alt-tabbed. New `window-handlers.ts` IPC + preload bridge + one call in the poll tick. Both tsc configs + vitest 245 green. | ~30 | `4bfcfa6` |
| 16 | **Ctrl/Cmd+Enter in the Playground prompt fires the contextual primary action** — Generate when idle, + Queue while rendering, Update while editing a queued shot. Plain Enter stays a newline (H3 prompts are multi-line, unlike Gen Space's Enter-submits one-liners). Keyboard flow now covers compose→queue→edit without touching the mouse. | ~10 | (this commit) |
